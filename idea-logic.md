# 项目逻辑链与答辩主线

> 版本：v2  
> 更新日期：2026-08-12  
> 用途：快速确认项目边界、实验因果链和课程答辩叙事。具体执行以 `proposal.md` 为准。

---

## 1. 一句话定位

> 指令视频编辑必须同时“按指令改对”和“保住不该改的内容”。本项目先建立经人工校准的成对质量尺子，再验证它能否改善 Best-of-N 和偏好训练，最后用压力测试研究欠编辑、过编辑等 reward hacking。

核心动作：

> **验尺子 → 用尺子选 → 用尺子训练 → 检查并防止骗尺子**

“验尺子”必须位于所有优化之前。未经人工校准的 reward 不能直接批量生成偏好训练对。

---

## 2. 项目真正研究什么

核心不是“再做一个 DPO”，而是三个连续问题：

1. **可信度**：自动 judge 真的能判断哪个编辑结果更好吗？
2. **有效性**：可信 reward 能否改善推理选择和模型单次采样？
3. **鲁棒性**：优化 reward 时是否会牺牲忠实或保持来骗分？

对应证据：

| 问题 | 实验 | 主要证据 |
| --- | --- | --- |
| 尺子准不准 | 人工校准 + judge 消融 | 成对准确率、换位一致率、排序相关性 |
| 选结果有没有用 | Best-of-N | 人工胜率、质量—成本曲线 |
| 模型是否真的变强 | DPO/LoRA | 单次采样对比、SFT control、统计显著性 |
| 是否会钻空子 | 权重/阈值压力测试 | 欠编辑率、过编辑率、reward–human gap、Pareto 前沿 |

---

## 3. 忠实—保持冲突

```text
保持过强                                        忠实过强
几乎不改                                          无关区域也被修改
   │                                                   │
   └── 欠编辑 ←── 理想：该改的改、其余保持 ──→ 过编辑 ──┘
```

- **指令忠实度**：目标修改是否正确发生；
- **源保持度**：非目标内容是否保持；
- **时序一致性**：帧间是否稳定；
- **视觉质量**：是否自然、无明显伪影。

简单加权和可能产生补偿：保持分很高可以掩盖完全没编辑，忠实分很高也可能掩盖无关区域被破坏。因此必须比较线性加权、短板型聚合、约束目标和 Pareto 排序。

---

## 4. 完整因果链

```text
① 现有编辑结果不稳定
   不忠实 / 过编辑 / 闪烁
        ↓
② 将问题分成忠实、保持、时序、视觉质量
        ↓
③ 先验证自动 judge 是否符合人类判断
   绝对打分 vs 成对比较 vs 双向换位
        ↓
④ 可信 reward 用于 Best-of-N
   N=1/2/4/8，先拿到低风险基本成果
        ↓
⑤ 用高置信偏好对训练一个可训练编辑主干
   Base / SFT control / Vanilla DPO / Proposed
        ↓
⑥ 主动施压检查 reward hacking
   欠编辑 / 过编辑 / reward–human gap
        ↓
⑦ 用非补偿式聚合、门控 KL、区域约束缓解
        ↓
⑧ 在独立测试集上完成最终评测
```

---

## 5. 修正后的创新定位

截至 2026 年，VIVA 已将相对奖励优化用于指令视频编辑，VEFX-Reward 也已经是视频编辑专用 reward。因此：

### 不再使用

- “首个面向指令视频编辑的偏好对齐框架”；
- “视频编辑没有专用 reward”；
- “该方向完全空白”。

### 改为验证

- 带 rubric、位置去偏和置信过滤的成对 reward 是否更可信；
- 忠实—保持冲突下，非补偿式 reward 是否优于普通加权和；
- 欠编辑率、过编辑率和 reward–human gap 是否构成有效诊断；
- 同一 reward 是否能同时支持 BoN 和偏好训练。

贡献强度必须由实验结果决定，不能预先把假设写成已证实结论。

---

## 6. 模型与数据边界

- **AnyV2V**：适合免训练推理和 Best-of-N，不默认作为 DPO 主干；
- **训练主干**：只选择一个能稳定训练、可访问损失且显存允许的编辑模型；
- **训练/开发数据**：DAVIS 或独立公开训练池；
- **最终测试**：IVEBench；
- **外部验证**：OmniEdit-Bench 或 VEFX-Bench；
- 禁止用 IVEBench 测试输入构造训练偏好对后，再汇报同一测试集结果。

第一阶段优先做目标区域明确的局部编辑。全局风格、相机运动等任务不强行套用相同的非编辑区域指标。

---

## 7. 两个交付节点

### M1：2026-09-01，课程答辩基本成果

最低闭环：

1. 跑通视频编辑和 reward pipeline；
2. 用小规模人评验证 judge；
3. 完成 $N=1,2,4,8$ 的 Best-of-N；
4. 给出主表、成本曲线、成功与失败案例；
5. 完成课程答辩 slides 初稿。

若时间允许，到 2026-09-08 补充类别分析、显著性和小规模 DPO sanity check。课程答辩不依赖正式 DPO 完成。

### M2：2026-10-06，八周完整成品

- 2,000–5,000 个高置信偏好对；
- 可复现的 LoRA/DPO checkpoint；
- Base、SFT control、Vanilla DPO、Proposed 对比；
- hacking 压力测试和缓解消融；
- IVEBench 独立测试与外部验证；
- 代码、配置、DEVLOG、报告、slides 和演示案例。

---

## 8. 三分钟答辩故事

> 指令视频编辑和视频生成不同：它不仅要把指令执行对，还要保住源视频中不该改变的内容。当前模型常在这两者之间失衡，要么几乎不改，要么改得太多。
>
> 我的切入点不是直接训练 DPO，而是先问：自动 reward 这把尺子到底准不准。我会用小规模人工偏好作为锚点，比较绝对打分、成对判断和双向换位的成对判断，并过滤不确定结果。
>
> 在课程答辩前，我先用这把尺子做 Best-of-N：同一输入生成多个结果，比较 N 从 1 到 8 时的质量和成本，形成一个免训练、可展示、可量化的基本成果。
>
> 后续再用高置信偏好对微调一个可训练编辑模型，检查它是否在单次采样下真正变强。最后主动改变忠实和保持的优化压力，观察是否出现欠编辑、过编辑和自动分数上涨但人类偏好下降，并测试多目标约束、门控 KL 和区域约束能否缓解。
>
> 因此项目是一条依赖清楚的证据链：先验尺子，再用尺子选和训练，最后验证优化是否会骗尺子。

---

## 9. 开工前速记

- 主线：**验 → 选 → 训 → 防**；
- 课程答辩保底：reward 校准 + Best-of-N；
- 完整项目主体：单次采样偏好训练；
- 亮点候选：忠实—保持冲突与 hacking 压力测试；
- 最大禁忌：reward 未校准就大规模构造训练对；
- 叙述边界：不再声称任务首创；
- 本地职责：写代码、mock/小样本测试、数据清单、结果分析、绘图和写作；
- A6000 职责：真实模型推理、批量候选生成、GPU 指标/本地 VLM、LoRA/DPO、权重扫描和完整 benchmark；
- 记录要求：不是每天汇总一次，而是**每完成一个可验证开发步骤就立即写入 `DEVLOG.md`，写完再做下一步**；
- 远程要求：A6000 作业提交前后都要记录实验 ID、代码版本、命令/配置、资源和产物路径。
