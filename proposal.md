# 面向忠实—保持冲突的鲁棒成对偏好视频编辑优化

**Robust Pairwise Preference Optimization for Instruction-Based Video Editing under Faithfulness–Preservation Conflicts**

> 版本：v4（实验执行版）  
> 更新日期：2026-08-12  
> 方向：多模态模型、视频编辑、偏好优化  
> 双阶段目标：**3–4 周形成课程答辩所需的基本成果，8 周完成训练、鲁棒性分析、完整评测与最终成品。**

---

## 0. 项目摘要

指令式视频编辑要求模型同时做到两件事：正确执行编辑指令，并保留源视频中不应改变的内容。现有模型常在这两者之间失衡，表现为欠编辑、过度编辑或时序不稳定。

本项目将视频编辑质量评估转化为可用于选择和训练的偏好信号，按以下依赖关系推进：

> **先验证尺子是否可信（Pairwise Reward）→ 用尺子选择结果（Best-of-N）→ 用尺子训练模型（Preference Optimization）→ 检查并缓解尺子被钻空子（Reward Hacking）**

与早期设想相比，本项目不再声称“首次把偏好优化用于指令视频编辑”。截至 2026 年，VIVA 等工作已经使用相对奖励优化视频编辑模型，VEFX-Reward 也已提供视频编辑专用奖励模型。因此，本项目的贡献聚焦于：

1. **可信成对奖励**：比较绝对打分与成对判断，显式处理位置偏差和判断不确定性；
2. **忠实—保持冲突建模**：避免简单加权和让一个维度补偿另一个维度；
3. **编辑型 reward hacking 诊断与缓解**：量化欠编辑、过编辑及 reward–human gap，并验证约束式优化是否有效；
4. **从免训练选择到训练后优化的完整证据链**：先用 Best-of-N 获得低风险结果，再验证偏好训练能否改善单次采样。

---

## 1. 问题定义与核心假设

### 1.1 任务

输入为源视频 $x$ 和自然语言编辑指令 $c$，编辑模型产生结果 $y$。高质量结果应满足：

- **指令忠实度** $r_f$：目标修改确实发生；
- **源保持度** $r_p$：非目标内容保持不变；
- **时序一致性** $r_t$：帧间稳定、运动连续；
- **视觉质量** $r_q$：无明显伪影且整体自然。

### 1.2 核心矛盾

- 只强调保持，模型可能几乎不编辑，形成**欠编辑**；
- 只强调忠实，模型可能修改无关区域，形成**过编辑**；
- 简单线性加权允许一个维度用极高分补偿另一个维度的失败，可能掩盖上述问题。

### 1.3 待检验假设

- **H1：Judge 可靠性。** 带 rubric、双向换位和置信过滤的成对判断，比绝对打分更符合人类偏好。
- **H2：推理时收益。** 可信 reward 能使 Best-of-N 相对单次采样获得稳定的人类偏好提升。
- **H3：训练时收益。** 基于高置信偏好对的训练能提升模型的单次采样质量，而不是仅提高自动 reward。
- **H4：Hacking 与缓解。** 线性 reward 在极端权重下会诱发欠编辑或过编辑；非补偿式聚合、门控 KL 或区域约束能降低其发生率。

---

## 2. 研究问题

- **RQ1：如何构建可信的编辑偏好信号？** 绝对打分、单向成对判断、双向成对判断和专用 reward 模型，哪一种与人类判断最一致？
- **RQ2：reward 能否直接改善推理结果？** 在相同候选池上，Best-of-N 是否带来显著且值得成本的提升？
- **RQ3：reward 能否改善模型本身？** 偏好优化后，模型在单次采样下能否优于原模型和等训练量对照？
- **RQ4：如何识别和缓解编辑型 reward hacking？** 欠编辑、过编辑和 reward–human gap 如何定义、诱发、测量与降低？

---

## 3. 相关工作与项目边界

### 3.1 视频编辑模型与基准

- **AnyV2V**：免训练、模块化，适合推理基线和 Best-of-N，但不是最自然的端到端 DPO 主干。
- **InsV2V / 其他可训练编辑模型**：候选训练主干，最终选择以代码可用性、显存和能否访问训练损失为准。
- **IVEBench**：主要独立测试基准，覆盖视频质量、指令合规和视频保真。
- **OmniEdit-Bench / VEFX-Bench**：作为外部鲁棒性验证候选，不参与 reward 调参。

### 3.2 偏好优化

- **Diffusion-DPO / VideoDPO / DenseDPO**：提供扩散偏好损失、分差重加权和片段级偏好信号等技术基础。
- **VIVA (CVPR 2026)**：已将相对奖励优化用于指令视频编辑，是必须纳入的直接相关工作。

### 3.3 编辑质量奖励

- 通用 MLLM judge 便于使用，但存在位置偏差、绝对分数不稳定和视觉捷径。
- **VEFX-Reward (2026)** 已是专门面向视频编辑的多维 reward；本项目不能仅以“四维加权评分”作为创新。

### 3.4 明确边界

本项目在 8 周内不追求：

- 构建大规模新数据集；
- 同时微调多个大型视频编辑模型；
- 覆盖所有编辑类型；
- 训练全新的通用视频 reward 模型。

第一阶段优先处理目标区域相对明确的属性编辑、对象编辑和局部视觉效果。全局风格、相机运动等不适合统一“非编辑区域”定义的任务，放到扩展评测。

---

## 4. 数据协议与防泄漏设计

### 4.1 数据划分

| 用途 | 数据来源 | 是否用于调参 |
| --- | --- | --- |
| 开发与训练 | DAVIS、公开训练视频或另建训练池 | 是 |
| 验证 | 与训练视频、指令模板分离的 validation split | 是 |
| 主测试 | IVEBench | 否，仅最终使用 |
| 外部验证 | OmniEdit-Bench 或 VEFX-Bench | 否 |

不得使用 IVEBench 测试输入生成偏好训练对后，再在同一输入上汇报最终性能。

### 4.2 初始规模

- 开发集：约 100 个“源视频 + 指令”输入；
- 每个输入：固定随机种子生成 4 个候选；
- 人工校准：优先覆盖 100 个输入，形成约 600–1000 个成对判断；
- 正式偏好训练：先构建 2,000–5,000 个高置信 pair，再根据训练收益决定是否扩展。

### 4.3 可复现要求

每个结果保存：数据 ID、指令、编辑类别、模型与 checkpoint、推理参数、随机种子、候选路径、各维 reward、judge 原始输出、代码版本和生成时间。

---

## 5. 方法

### 5.1 Layer 0：可信成对 Reward

#### 分维度判断

- $r_f$：指令是否得到正确执行；
- $r_p$：非目标内容是否保持；
- $r_t$：时序是否稳定；
- $r_q$：视觉质量是否自然。

局部编辑优先使用目标 mask 或检测/分割区域：目标区域衡量编辑成功，非目标区域衡量保持。若无法可靠定位目标区域，该样本单独标记，不与局部编辑共享同一保持公式。

#### 成对判断与位置去偏

对候选 $A,B$：

1. 用同一 rubric 判断一次 $(A,B)$；
2. 交换位置再判断 $(B,A)$；
3. 两次结论一致才视为高置信偏好；
4. 不一致时标记为不确定，降低权重或不进入训练集。

当一个输入有多个候选时，使用以下两种方式比较：

- 小 $N$：全量两两比较，再用 Bradley–Terry 模型拟合候选效用；
- 大 $N$：锦标赛或瑞士轮排序，控制 MLLM 调用成本。

#### Reward 聚合候选

至少比较：

1. 线性加权和：$R_{sum}=\sum_i w_i r_i$；
2. 调和或几何平均：惩罚短板；
3. 最小值 / Tchebycheff：限制最弱维度；
4. 约束目标：最大化忠实度，同时要求保持度、时序和质量超过阈值；
5. Pareto 排序：保留不同忠实—保持权衡下的非支配候选。

### 5.2 Layer 1：Best-of-N

同一输入生成 $N$ 个候选，由 reward 选择最终输出。比较 $N=1,2,4,8$，并报告质量提升与推理成本的关系。

所有排序方法必须复用相同候选池，避免把随机生成差异误判为 reward 的贡献。

### 5.3 Layer 2：偏好优化

训练主干选择一个可训练的视频编辑模型；AnyV2V 保留为推理 baseline。训练对包括：

- **Base**：原始模型；
- **LoRA/SFT control**：相同训练量但不使用偏好损失；
- **Vanilla DPO**：简单偏好构对；
- **Margin-weighted DPO**：按可信偏好差距加权；
- **Proposed**：双向去偏、置信过滤和冲突感知构对。

先用小数据做过拟合与损失 sanity check，确认 chosen/rejected 梯度方向正确后再扩大训练。核心评测使用单次采样，Best-of-N 只作额外推理增强。

### 5.4 Layer 3：Reward Hacking 诊断与缓解

通过系统调整忠实与保持权重主动进行压力测试，而不是预设 hacking 一定存在。

- **欠编辑率**：保持度高于阈值，但忠实度低于阈值；
- **过编辑率**：忠实度达标，但非目标区域保持度低于阈值；
- **reward–human gap**：自动 reward 上升，但人工总体偏好下降的样本比例。

缓解方案按增量消融：

1. 非补偿式多目标聚合；
2. 基于 judge 分歧或预测熵的不确定性门控 KL；
3. 针对非目标区域的图像空间约束。

若未观察到统计显著的 hacking，应报告负结果和适用边界，不强行宣称缓解有效。

---

## 6. 实验矩阵

### E0：系统与基线验证

目的：证明数据、推理、缓存、指标和复现链路可用。

退出条件：

- 至少 50 个输入稳定生成；
- 四个 reward 分量均有有效输出；
- 固定种子可复现；
- IVEBench 评测代码完成 smoke test。

### E1：Judge 可靠性

比较绝对打分、单向成对、双向成对、带 rubric 的分维度成对，以及可用的专用 reward。

指标：成对准确率、Kendall $\tau$ / Spearman、换位一致率、平局率、分类别表现。

建议进入下一阶段的门槛：总体成对准确率不低于 70%，换位一致率不低于 85%。若不达标，先修 reward。

### E2：Best-of-N

比较随机选择、线性加权、成对锦标赛、鲁棒多目标排序，并绘制 $N=1,2,4,8$ 的质量—成本曲线。

基本成果目标：$N=4$ 相对 $N=1$ 的人工总体胜率达到 60% 左右，且忠实度或保持度没有显著退化。

### E3：偏好优化

比较 Base、LoRA/SFT control、Vanilla DPO、Margin-weighted DPO 和 Proposed。正式关键配置尽量使用 3 个随机种子，报告均值、方差、峰值显存、训练时间与推理成本。

### E4：Hacking 压力测试

扫描 reward 权重或约束阈值，绘制忠实—保持 Pareto 前沿，报告欠编辑率、过编辑率和 reward–human gap。

### E5：完整评测

- IVEBench 三维及其分项指标；
- 独立于训练 reward 的外部 evaluator；
- 少量人工总体偏好及分维度偏好；
- 分编辑类别结果；
- bootstrap 95% 置信区间和配对显著性检验；
- 定量表、Pareto 图、典型成功与失败案例。

---

## 7. 双阶段交付计划

起始日期按 **2026-08-12** 计算。课程答辩的精确日期尚未写入，因此以 9 月初为硬窗口：争取 W3 结束形成基本成果，W4 用于补实验和打磨展示。

### 7.1 课程答辩基本成果：3–4 周

| 周次 | 日期 | 任务 | 当周必须产出 |
| --- | --- | --- | --- |
| W1 | 08-12 ～ 08-18 | 环境与数据协议；跑通 1 个推理 baseline；实现 reward 接口和缓存 | 50 个可复现候选结果；pipeline 流程图；基线案例 |
| W2 | 08-19 ～ 08-25 | 人工校准集；比较绝对打分、单向成对、双向成对；确定排序实现 | Judge 可靠性表；位置偏差统计；最终 reward v0 |
| W3 | 08-26 ～ 09-01 | $N=1,2,4,8$ 的 Best-of-N；质量—成本分析；初步案例整理 | **课程答辩最低可交付：BoN 主表/曲线、人工偏好结果、成功失败案例** |
| W4 | 09-02 ～ 09-08 | 补齐显著性、类别分析和 slides；并行准备小规模 DPO sanity check | **课程答辩增强版：完整基本实验 + 方法图 + 初步训练迹象** |

课程答辩不应依赖 DPO 完成。最低闭环是：

> 问题定义 → reward 可靠性验证 → Best-of-N 显著改善 → 初步 hacking/失败案例 → 后续训练计划。

### 7.2 八周完整成品

| 周次 | 日期 | 任务 | 最终产出 |
| --- | --- | --- | --- |
| W5 | 09-09 ～ 09-15 | 构建 2,000–5,000 个高置信偏好对；完成小规模 DPO 验证 | 数据统计、训练曲线、可用 checkpoint |
| W6 | 09-16 ～ 09-22 | 正式 LoRA/DPO 训练；完成核心 baseline 和消融 | Base/DPO/Proposed 单次采样对比 |
| W7 | 09-23 ～ 09-29 | Hacking 压力测试、缓解消融、IVEBench 最终评测 | Pareto 图、hacking 统计、完整主表 |
| W8 | 09-30 ～ 10-06 | 外部验证、统计检验、代码整理、报告和演示 | **完整成品：代码、配置、结果、报告、slides、演示案例** |

### 7.3 里程碑定义

**M1：2026-09-01，基本成果**

- 可复现的视频编辑与 reward pipeline；
- 一份小规模人工校准结果；
- Best-of-N 主实验与成本曲线；
- 至少一组可信的定量提升和可视化案例；
- 课程答辩 slides 初稿。

**M2：2026-10-06，完整成品**

- 偏好训练模型与 checkpoint；
- 完整对比、消融和统计检验；
- hacking 压力测试与缓解结论；
- 独立测试集结果；
- 可运行代码、配置、实验记录、报告和演示材料。

---

## 8. 本地开发与 A6000 计算分工

开发采用“**本地写代码与轻量验证，学校 A6000 执行模型和批量实验**”的方式。A6000 为 48GB 显存，但是否能装下具体模型仍取决于参数量、分辨率、帧数、精度和训练方式；不能仅因使用 A6000 就默认所有配置都可运行。

### 8.1 优先在本地完成

- 文档、实验协议、prompt rubric 和配置文件编写；
- 代码结构、数据类、缓存、日志、结果解析和异常处理；
- 使用 mock 或极小样本的单元测试、静态检查和 CPU smoke test；
- 数据清单、train/validation/test split 和泄漏检查；
- 已生成结果的表格汇总、统计检验、绘图、报告和 slides；
- 调用云端 MLLM API 的 judge 逻辑开发。如果 judge 通过远程 API 推理，本地只承担请求、缓存和结果处理，不需要占用 A6000。

本地测试的目标是尽量在提交远程任务前发现代码错误，但本地 mock 通过不等于真实模型链路已经验证。

### 8.2 必须或强烈建议使用 A6000

| 场景 | 为什么需要 A6000 | 对应阶段 |
| --- | --- | --- |
| 首次加载真实视频编辑模型并完成端到端推理 | 视频扩散/流模型的权重、中间激活和时序模块通常需要较大显存 | E0 |
| 为每个输入批量生成 4–8 个候选视频 | Best-of-N 的主要成本是重复视频推理，本地不适合批量跑 | E0–E2 |
| 批量运行 CLIP/视频编码器、光流、分割或其他 GPU 指标 | 单个样本可能可在本地运行，但全数据特征提取耗时大 | E1–E5 |
| 使用开源 MLLM/VLM 在本地部署 judge | 视频/多图输入会显著增加显存和推理时间 | E1、偏好数据构建 |
| 生成 2,000–5,000 个偏好训练 pair 所需的候选 | 候选生成是训练前最大的计算开销之一 | E3 数据准备 |
| LoRA/SFT control、DPO sanity check 与正式 DPO 训练 | 需要加载训练模型、参考模型或额外前向，并保存梯度和优化器状态 | E3 |
| Hacking 权重扫描和多随机种子实验 | 需要重复推理或训练，计算量按配置数量增长 | E4 |
| IVEBench 及外部 benchmark 的完整模型推理 | 独立测试涉及大量视频和多配置比较 | E5 |

### 8.3 不必立即使用 A6000 的情况

- 只是在检查数据路径、JSON/CSV 格式或采样逻辑；
- 只对已有指标结果做统计和画图；
- 修改 prompt 后只检查模板渲染，不执行真实 judge；
- 使用云端 MLLM API 进行判断；
- 编写或测试调度、断点续跑和缓存逻辑，且可用 mock 输入验证。

### 8.4 远程作业前后的最小记录

每个 A6000 作业使用唯一实验 ID 和独立输出目录。提交前在 `DEVLOG.md` 记录：

- 本地代码版本或 commit、远程代码快照；
- 模型/checkpoint、数据 split、样本数和随机种子；
- 完整命令或配置文件路径；
- 精度、分辨率、帧数、batch size、采样步和显存策略；
- 预计运行时间及产物路径。

作业完成或中断后立即记录：状态、运行时间、峰值显存、核心指标、日志与产物路径、失败原因和下一步。先用 1–2 个样本做真实 GPU smoke test，再提交批量任务；正式训练前先完成小规模 overfit/sanity check。

---

## 9. 风险、降级顺序与决策门

| 风险 | 触发信号 | 处理方式 |
| --- | --- | --- |
| baseline 无法稳定运行 | W1 中期仍不能批量生成 | 立即切换到已验证可运行的单一模型，缩短视频和降低分辨率 |
| MLLM judge 不可靠 | E1 未达 70% 准确率或 85% 换位一致率 | 加强 rubric、过滤不确定 pair、引入专用 reward；暂停 DPO |
| MLLM 调用成本过高 | 两两比较成为主要瓶颈 | 由全比较切换到锦标赛/瑞士轮，缓存所有判断 |
| DPO 显存或时间不足 | W5 仍无法完成小规模 sanity check | 降低分辨率/帧数、LoRA、单主干；完整成果降级为 BoN + reward/hacking 研究 |
| DPO 自动指标升但人评降 | reward–human gap 上升 | 停止扩大训练，优先做 reward 修正和约束式构对 |
| hacking 不显著 | 压力测试没有稳定退化 | 报告负结果；将贡献改为 reward 可靠性和冲突前沿分析 |

工作优先级不可倒置：**E0 → E1 → E2 → E3 → E4/E5**。只有 reward 通过人工校准后，才允许用它批量构造训练偏好对。

---

## 10. 最终贡献表述

若实验全部支持假设，建议使用以下表述：

1. 建立一个面向指令视频编辑的分维度成对偏好协议，量化并降低 MLLM judge 的位置偏差与不确定性；
2. 系统比较补偿式与非补偿式 reward 聚合，揭示忠实—保持冲突对候选选择和偏好训练的影响；
3. 定义欠编辑率、过编辑率和 reward–human gap，并通过压力测试评估鲁棒优化策略；
4. 在严格数据隔离下，验证该偏好信号对 Best-of-N 和模型单次采样质量的作用。

避免使用“首个视频编辑偏好对齐”“此前没有视频编辑 reward”等容易被新文献反驳的绝对表述。

---

## 11. 主要参考文献

1. AnyV2V: A Tuning-Free Framework for Any Video-to-Video Editing Tasks. TMLR 2024.
2. InstructVid2Vid: Controllable Video Editing with Natural Language Instructions. 2023.
3. Diffusion-DPO: Diffusion Model Alignment Using Direct Preference Optimization. 2023.
4. VideoDPO: Omni-Preference Alignment for Video Diffusion Generation. CVPR 2025.
5. DenseDPO: Fine-Grained Temporal Preference Optimization for Video Diffusion Models. NeurIPS 2025.
6. IVEBench: Modern Benchmark Suite for Instruction-Guided Video Editing Assessment. ICLR 2026.
7. VIVA: VLM-Guided Instruction-Based Video Editing with Reward Optimization. CVPR 2026.
8. VEFX-Bench: A Holistic Benchmark for Generic Video Editing and Visual Effects. 2026.
9. OmniEdit-Bench: A Comprehensive Benchmark for Instruction-Based Video Editing. 2026.
